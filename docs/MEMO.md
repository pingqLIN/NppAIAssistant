# Project MEMO

This file records deferred product and UX ideas. Items here are **not part of the current release scope** unless promoted into a separate reviewed task.

## Deferred provider-settings UX ideas

Recorded during the v0.2.0.6 release-candidate review. Do not implement these as part of the current release:

1. Move AI provider API configuration to the second Settings page.
2. Re-evaluate the information architecture because **Default Provider** and provider configuration are currently split across two pages; consider whether they should be consolidated or rearranged.
3. In the provider API page, consider a dedicated **Local / Compatible Services** section with additional options for local and OpenAI-compatible endpoints.

Current v0.2.0.6 scope remains unchanged: preserve the existing **AI Services** and **Prompt Behavior** structure, aside from explicitly approved small release fixes.
